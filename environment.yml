name: chap9rag
channels:
  - conda-forge
dependencies:
  - python=3.10
  - faiss-cpu=1.7.4
  - pandas
  - fastapi
  - pip
  - pip:
      - langchain>=0.2.0
      - langchain-community>=0.2.0
      - langchain-mistralai
      - langchain-text-splitters
      - sentence-transformers
      - mistralai==1.5.1        # dernière v1 stable, compatible instructor + ragas
      - python-dotenv
      - ragas==0.4.3
      - datasets
      - uvicorn