name: chap9rag
channels:
  - conda-forge
dependencies:
  - python=3.10
  - faiss-cpu=1.7.4
  - pandas
  - fastapi
  - pip
  - pip:
      - langchain>=0.2.0
      - langchain-community>=0.2.0
      - langchain-mistralai
      - langchain-text-splitters
      - sentence-transformers
      - mistralai==0.4.2         # pip uniquement, plus dans conda
      - python-dotenv
      - ragas
      - datasets
      - uvicorn